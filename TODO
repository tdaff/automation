TODO(tdaff):

Generic running, not just wooki
Automatic continuation
cpmd support (pseudopotentials)
cif file input
vdw custom potentials and choice
directories; where do the jobs run?
guest library and choice

